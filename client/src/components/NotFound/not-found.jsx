import React from 'react';
import Text from '../text/text';

const NotFound = (props) => {
    return (
        <div className='rating'>
            <Text className='number'>Not Found</Text>
        </div>
    )
};

export default NotFound;
